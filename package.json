{
  "name": "linkedlab-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "SITE_URL=https://linkedlab.eu node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "deploy:cloudflare": "npm run build && wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
